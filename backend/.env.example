SUPABASE_URL=
SUPABASE_KEY=
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash    # default for all agents; overridden per-agent below
ORGANIZER_MODEL=                  # Agent 1 — classify/tag/summarise (leave blank to use GEMINI_MODEL)
RESEARCHER_MODEL=                 # Agent 2 — extract search queries  (leave blank to use GEMINI_MODEL)
SEARCH_MODEL=                     # Agent 3 — interpret search queries (leave blank to use GEMINI_MODEL)
OPENAI_API_KEY=
YOUTUBE_API_KEY=
REDIS_URL=redis://localhost:6379/0
