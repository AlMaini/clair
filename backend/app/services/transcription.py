async def transcribe_audio(file_bytes: bytes) -> str:
    """Transcribe audio bytes to text using OpenAI Whisper API.

    Args:
        file_bytes: Raw audio file bytes (mp3, wav, m4a, etc.)

    Returns:
        Transcribed text string.
    """
    return ""
